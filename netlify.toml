[build]
  base = "apps/web"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_API_URL = "https://api.smartfieldwork.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
